{
  "name": "myblog",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "18.2.74"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint && tsc --noEmit && bun eslint . --ext .ts,.tsx --max-warnings=0 && bun prettier --check .",
    "lint:type": "tsc --noEmit",
    "lint:eslint": "eslint . --ext .ts,.tsx --max-warnings=0",
    "lint:prettier": "prettier --check .",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "next": "^14.1.4",
    "prettier": "^3.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
